server {
	listen 80;

	access_log <%= node[:chub-nginx][:log_dir] %>/files_access.log;
	error_log <%= node[:chub-nginx][:log_dir] %>/files_error.log;

	keepalive_timeout 10;
	root [:chub-nginx][:www_home];

}