{
  "mailer": {
    "mail_from": "<%= node[:chub_sensu_sso][:email][:sender] %>",
    "mail_to": "<%= node[:chub_sensu_sso][:email][:recipient] %>",
    "smtp_address": "<%= node[:chub_sensu_sso][:email][:smtp_server] %>",
    "smtp_port": "<%= node[:chub_sensu_sso][:email][:smtp_port] %>",
    "smtp_domain": "<%= node[:chub_sensu_sso][:email][:smtp_domain] %>"
  }
}
