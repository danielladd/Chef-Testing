<?xml version="1.0" encoding="UTF-8"?>

<project>
  <node name="<%= node[:fqdn] %>" description="Rundeck server node" tags="" hostname="<%= node[:fqdn] %>" osArch="amd64" osFamily="unix" osName="<%= node[:os] %>" osVersion="<%= node[:os_version] %>" username="rundeck-ssh"/>
<% if @nodes %>
<% @nodes.each do |box| %>
  <node name="<%= box[:name] %>" <% if box[:winrm] %>node-executor="overthere-winrm" winrm-password-option="option.winrmPassword"<% end %> description="<%= box[:description] %>" tags="<%= box[:tags] %>" hostname="<%= box[:hostname] %>" osArch="<%= box[:osArch] %>" osFamily="<%= box[:osFamily] %>" osName="<%= box[:osName] %>" osVersion="<%= box[:osVersion] %>" username="<%= box[:username] %>"/>
<% end %>
<% end %>
</project>
