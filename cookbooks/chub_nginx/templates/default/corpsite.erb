# CREATED BY CHEF
# Changes made locally will be overridden

server {
	listen		80;
	server_name	www.<%= @site %>.nexus.commercehub.com;
	rewrite ^/(.*) http://<%= @site %>.nexus.commercehub.com/$1 permanent;

}

server {
	listen		80;
	server_name	<%= @site %> <%= @site %>.nexus.commercehub.com;

	access_log	/var/log/nginx/<%= @site %>.access.log;
	error_log	/var/log/nginx/<%= @site %>.error.log;

	location / {
		root	/var/www/<%= @site %>;
		index	homepage.html index.html index.htm index;

	}

}